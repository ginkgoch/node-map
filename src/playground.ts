import { RTIndex, RTRecordType, ShapefileFeatureSource, FeatureLayer, FillStyle, Render } from '.';
import fs from 'fs';
import _ from 'lodash';
import canvas from 'canvas';
import { NativeFactory } from './native';

NativeFactory.registerFrom(canvas);

async function main() {
    
}

main();